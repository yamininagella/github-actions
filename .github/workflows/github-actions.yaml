name: Merge Develop branch to Main
on:
  push:
    branches:
      - 'develop'
jobs:
  merge-develop-to-main:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - name: print maessage
        run: echo "✅ Starting process to merge Develop into Main on the ${{ github.repository }} by @${{ github.actor }}."
   
